/**:
  ros__parameters:
    # Initial values measured in the transformed bike pose.
    # Recalibrate these values on the physical robot before high-speed driving.
    wheel_radius: 0.0309258
    wheel_separation: 0.125
    max_wheel_speed: 20.0
    command_timeout: 0.5

    # Conservative ver5 measurement retained for ver6 because it is larger than
    # the ver6 mesh-only estimate. Re-measure after the physical bike pose is verified.
    footprint: "[[0.2535, 0.0776], [0.2535, -0.2305], [-0.3510, -0.2305], [-0.3510, 0.0776]]"
